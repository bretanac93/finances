{% extends 'layout2.html.twig' %}

{% block body %}
  <div class="page-content">
        <!-- BEGIN SAMPLE PORTLET CONFIGURATION MODAL FORM-->
        <div id="portlet-config" class="modal hide">
            <div class="modal-header">
                <button data-dismiss="modal" class="close" type="button"></button>
                <h3>Widget Settings</h3>
            </div>
            <div class="modal-body"> Widget settings form goes here</div>
        </div>
        <div class="clearfix"></div>
        <div class="content">

        <div class="page-title">
            <h3>Ingresos</h3>
        </div>

        <div class="add-new">
            <a href="{{ path('income_new') }}" title=""><i class="fa fa-plus"><span id="icon-new">Crear Nuevo</span></i></a>
        </div>

        <table class="table table-hover no-more-tables">
        
            <thead>
            
                <tr>
                    <th>#</th>
                    <th>Entrada</th>
                    <th>Razon de Entrada</th>
                    <th>Importe</th>
                    <th>Descripcion</th>
                </tr>
            </thead>
            {% for income in incomes %}
            <tbody>
                <tr>
                    <td>{{ income.id }}</td>
                    <td>{{ income.cashEntry }}</td>
                    <td>{{ income.entryReason }}</td>
                    <td>{{ income.amount }}</td>
                    <td>{{ income.briefDescription }}</td>
                </tr>
            </tbody>
            {% endfor %}
        </table>
        
        </div>
    </div>
{% endblock %}
