{% set active = 'plan financiero' %}

{% extends 'layout2.html.twig' %}

{% block body %}
    <div class="page-content">
        <!-- BEGIN SAMPLE PORTLET CONFIGURATION MODAL FORM-->
        <div id="portlet-config" class="modal hide">
            <div class="modal-header">
                <button data-dismiss="modal" class="close" type="button"></button>
                <h3>Widget Settings</h3>
            </div>
            <div class="modal-body"> Widget settings form goes here </div>
        </div>
        <div class="clearfix"></div>
        <div class="content">
            <div class="row-fluid">
                <div class="span12">
                    <div class="grid simple ">
                        <div class="grid-title">
                            <h3>Ingresos</h3>
                            <!--<div class="tools"> <a href="javascript:;" class="collapse"></a> <a href="#grid-config" data-toggle="modal" class="config"></a> <a href="javascript:;" class="reload"></a> <a href="javascript:;" class="remove"></a> </div>-->
                            <div class="add-new btn btn-primary">
                                <a href="{{ path('income_new') }}"><i class="fa fa-plus"><span id="icon-new">Crear Nuevo</span></i></a>
                            </div>
                        </div>

                        <div class="grid-body">

                            <table id="example" class="table table-striped table-bordered nowrap mydatatable" cellspacing="0" width="100%">
                                <thead>
                                <tr>
                                    <th>#</th>
                                    <th>Entrada</th>
                                    <th>Razon de Entrada</th>
                                    <th>Importe</th>
                                    <th>Descripcion</th>
                                    <th>Acciones</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for income in incomes %}
                                    <tr>
                                        <td>{{ income.id }}</td>
                                        <td>{{ income.cashEntry }}</td>
                                        <td>{{ income.entryReason }}</td>
                                        <td>{{ income.amount }}</td>
                                        <td>{{ income.briefDescription }}</td>
                                        <td>
                                            <a href="{{ path('income_edit',{ 'id':income.id }) }}"><i class="fa fa-pencil edit_pencil"></i></a>
                                            <a href="{{ path('income_delete',{ 'id':income.id }) }}"><i class="fa fa-times delete_times"></i></a>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}















