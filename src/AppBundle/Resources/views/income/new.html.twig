{% extends 'layout2.html.twig' %}

{% block body %}
<div class="page-content">
        <!-- BEGIN SAMPLE PORTLET CONFIGURATION MODAL FORM-->
        <div id="portlet-config" class="modal hide">
            <div class="modal-header">
                <button data-dismiss="modal" class="close" type="button"></button>
                <h3>Widget Settings</h3>
            </div>
            <div class="modal-body"> Widget settings form goes here</div>
        </div>
        <div class="clearfix"></div>
        <div class="content">

            <div class="page-title">
                <h3>Nuevo Ingreso</h3>
            </div>

            <form id="form" method="post" action="{{ path('income_new') }}">

                
                <div class="form-group">
                    <label class="form-label">Entrada de Efectivo</label>
                    <span class="help">e.g. "Balance Genral"</span>
                    <div class="  right">
                        <i class=""></i>
                        <select id="appbundle_icome_cashEntry" name="appbundle_income[cashEntry]" class="select2 form-control">
                            {% for childAccount in childAccounts %}

                                    <option value="{{ childAccount.name }}">
                                        {{ childAccount.name }}
                                    </option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label">Razon de Entrada</label>
                    <span class="help">e.g. "Salario Neto"</span>
                    <div class="  right">
                        <i class=""></i>
                        <select id="appbundle_income_entryReason" name="appbundle_income[entryReason]" class="select2 form-control">
                            {% for childAccount in childAccounts %}

                                    <option value="{{ childAccount.name }}">
                                        {{ childAccount.name }}
                                    </option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label">Importe</label>
                    <span class="help">e.g. "250,000,000"</span>
                    <div class="input-with-icon  right">
                        <i class=""></i>
                        <input type="text" id="appbundle_income_amount" name="appbundle_income[amount]" required="required" class="form-control">
                    </div>
                </div>
                <div class="form-group">
                    <label class="form-label">Breve Descripcion</label>
                    <span class="help">e.g. "Mi Cheque"</span>
                    <div class="input-with-icon  right">
                        <i class=""></i>
                        <textarea id="appbundle_income_briefDescription" name="appbundle_income[briefDescription]" required="required" class="form-control"></textarea>
                        
                    </div>
                </div>
                <div class="form-actions">
                    <div class="pull-right">
                        <input type="submit" value="Crear" class="btn btn-success btn-cons"><i class="icon-ok"></i>
                    </div>
                </div>
            </form>


        </div>
    </div>
{% endblock %}
