{% extends 'layout2.html.twig' %}

{% block body %}
  <div class="page-content">
        <!-- BEGIN SAMPLE PORTLET CONFIGURATION MODAL FORM-->
        <div id="portlet-config" class="modal hide">
            <div class="modal-header">
                <button data-dismiss="modal" class="close" type="button"></button>
                <h3>Widget Settings</h3>
            </div>
            <div class="modal-body"> Widget settings form goes here</div>
        </div>
        <div class="clearfix"></div>
        <div class="content">

        <div class="page-title">
            <h3>Cuentas Madres</h3>
        </div>

        <div class="add-new">
            <a href="{{ path('debit_new') }}" title=""><i class="fa fa-plus"><span id="icon-new">Crear Nuevo</span></i></a>
        </div>

        <table class="table table-hover no-more-tables">
        
            <thead>
                <tr>
                    <th>#</th>
                    <th>Codigo</th>
                    <th>Nombre</th>
                    <th>Tipo de Cuenta</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            {% for matrixAccount in matrixAccounts %}
            <tbody>
                <tr>
                    <td>{{ matrixAccount.id }}</td>
                    <td>{{ matrixAccount.code }}</td>
                    <td>{{ matrixAccount.name }}</td>
                    <td>{{ matrixAccount.accountType }}</td>
                    <td><a href="{{ path('matrixaccount_edit',{ 'id':matrixAccount.id }) }}"><i class="fa fa-pencil" id="edit_pencil"></i></a>

                    <a href="{{ path('matrixaccount_delete',{ 'id':matrixAccount.id }) }}"><i class="fa fa-times" id="delete_times"></i></a>
                    </td>
                </tr>
            </tbody>
            {% endfor %}
        </table>
        
        </div>
    </div>
{% endblock %}
