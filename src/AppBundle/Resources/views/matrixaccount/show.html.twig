{% set active = 'grupo cuentas' %}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Matrixaccount</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ matrixAccount.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ matrixAccount.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ matrixAccount.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('matrixaccount_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('matrixaccount_edit', { 'id': matrixAccount.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
