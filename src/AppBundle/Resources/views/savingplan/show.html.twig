{% extends 'base.html.twig' %}

{% block body %}
    <h1>Savingplan</h1>

    <table>
        <tbody>
            <tr>
                <th>Savingtargetaccount</th>
                <td>{{ savingPlan.savingTargetAccount }}</td>
            </tr>
            <tr>
                <th>Openingbalance</th>
                <td>{{ savingPlan.openingBalance }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ savingPlan.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('savingplan_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('savingplan_edit', { 'id': savingPlan.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
